# Nyaya backend - runtime dependencies, fully pinned.
# A build that resolves different packages next week is not a build.
#
# NOTE: no torch, no sentence-transformers. Embeddings and reranking run on
# onnxruntime via fastembed, which keeps the image ~500MB instead of ~2.5GB
# and avoids pulling CUDA wheels onto a CPU-only host.

# ---- web ----
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.19
sse-starlette==2.1.3

# ---- config / validation ----
pydantic==2.10.3
pydantic-settings==2.7.0

# ---- database + vector store ----
SQLAlchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6
greenlet==3.1.1

# ---- queue / cache / rate limiting ----
redis==5.2.1
arq==0.26.3
slowapi==0.1.9

# ---- PDF parsing ----
# Deliberately permissive licences only. PyMuPDF is AGPL and is avoided:
#   pdfplumber  MIT       char-level geometry, layout analysis
#   pypdf       BSD       page copying for form extraction
#   pypdfium2   BSD/Apache rasterisation feeding OCR
pdfplumber==0.11.10
pypdf==6.16.2
pypdfium2==4.30.1

# ---- OCR fallback ----
pytesseract==0.3.13
opencv-python-headless==4.10.0.84
Pillow==11.0.0

# ---- vector store client ----
qdrant-client==1.12.2

# ---- embeddings + rerank (open weights, local inference) ----
fastembed==0.5.1

# ---- LLM providers (HTTP only; swapped by env var) ----
httpx==0.28.1

# ---- observability ----
structlog==24.4.0
prometheus-client==0.21.1
prometheus-fastapi-instrumentator==7.0.2

# ---- misc ----
python-magic==0.4.27
tenacity==9.0.0
