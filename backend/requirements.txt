# Nyaya backend - runtime dependencies, fully pinned.
# A build that resolves different packages next week is not a build.
#
# Every version here is one the system was actually verified against, not one
# recalled from memory. The first image build failed because Pillow was pinned
# to 11.0.0 while pdfplumber 0.11.10 requires >=12.2.0 - exactly the class of
# error pinning exists to surface at build time rather than at run time.
#
# No torch, no sentence-transformers. Embeddings and reranking run on
# onnxruntime via fastembed, which keeps the image small and avoids pulling
# CUDA wheels onto a CPU-only host.

# ---- web ----
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
sse-starlette==3.4.8

# ---- config / validation ----
pydantic==2.12.5
pydantic-settings==2.15.0

# ---- database ----
SQLAlchemy==2.0.52
asyncpg==0.31.0
alembic==1.19.1
greenlet==3.5.5
# only used by the alternate VectorStore backend; Qdrant is the default
pgvector==0.5.0

# ---- queue / cache / rate limiting ----
redis==5.3.1
arq==0.28.0
slowapi==0.1.10

# ---- PDF parsing ----
# Deliberately permissive licences only. PyMuPDF is AGPL and is avoided:
#   pdfplumber  MIT         char-level geometry, layout analysis
#   pypdf       BSD         page copying for form extraction
#   pypdfium2   BSD/Apache  rasterisation feeding OCR
pdfplumber==0.11.10
pypdf==6.16.2
pypdfium2==5.13.0
Pillow==12.3.0

# ---- OCR fallback ----
pytesseract==0.3.13
opencv-python-headless==5.0.0.93

# ---- vector store ----
qdrant-client==1.19.0

# ---- embeddings + rerank (open weights, local ONNX inference) ----
fastembed==0.8.0
onnxruntime==1.29.0

# ---- LLM providers (HTTP only; swapped by env var) ----
httpx==0.28.1

# ---- observability ----
structlog==26.1.0
prometheus-client==0.26.0
prometheus-fastapi-instrumentator==8.1.0

# ---- misc ----
tenacity==9.1.4
