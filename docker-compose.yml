# Nyaya - full stack. `docker compose up` from a clean clone brings up everything.
# Bootstrap (ingest the act + extract the forms) is a separate one-shot:
#   docker compose run --rm bootstrap
name: nyaya

x-postgres-env: &postgres-env
  POSTGRES_USER: ${POSTGRES_USER:-nyaya}
  POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-nyaya}
  POSTGRES_DB: ${POSTGRES_DB:-nyaya}

services:
  qdrant:
    # Vector store. The brief prefers Qdrant; its Query API does dense+sparse
    # prefetch with server-side RRF fusion, so hybrid retrieval is a query
    # rather than hand-rolled fusion code. Dashboard: :6333/dashboard
    image: qdrant/qdrant:v1.12.5
    volumes:
      - qdrantdata:/qdrant/storage
    ports:
      - "6333:6333"
    healthcheck:
      # the image is distroless: no curl, no shell utilities. Qdrant ships its
      # own healthcheck binary for exactly this.
      test: ["CMD", "/qdrant/qdrant", "--version"]
      interval: 5s
      timeout: 5s
      retries: 12
    restart: unless-stopped
    networks: [nyaya]

  postgres:
    # pgvector ships as an extension on this image. Kept even though Qdrant
    # holds the vectors: the alternate VectorStore backend needs it, and it
    # costs nothing to have available.
    image: pgvector/pgvector:pg16
    environment: *postgres-env
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-nyaya} -d ${POSTGRES_DB:-nyaya}"]
      interval: 5s
      timeout: 5s
      retries: 12
    restart: unless-stopped
    networks: [nyaya]
    ports:
      - "${POSTGRES_PORT_HOST:-5432}:5432"

  redis:
    image: redis:7.4-alpine
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redisdata:/data
    ports:
      # Host access only; services on the compose network use redis:6379.
      - "${REDIS_PORT_HOST:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 12
    restart: unless-stopped
    networks: [nyaya]

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: runtime
    env_file: [.env]
    environment:
      POSTGRES_HOST: postgres
      REDIS_HOST: redis
    volumes:
      - ./data:/data
      - modelcache:/home/nyaya/.cache
    ports:
      - "${NYAYA_API_PORT:-8000}:8000"
    depends_on:
      postgres: {condition: service_healthy}
      redis: {condition: service_healthy}
      qdrant: {condition: service_healthy}
    restart: unless-stopped
    networks: [nyaya]

  worker:
    # Same image, different entrypoint - one build, two roles.
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: runtime
    command: ["arq", "app.workers.tasks.WorkerSettings"]
    env_file: [.env]
    environment:
      POSTGRES_HOST: postgres
      REDIS_HOST: redis
    volumes:
      - ./data:/data
      - modelcache:/home/nyaya/.cache
    depends_on:
      postgres: {condition: service_healthy}
      redis: {condition: service_healthy}
      qdrant: {condition: service_healthy}
    restart: unless-stopped
    networks: [nyaya]

  bootstrap:
    # One-shot: run migrations, ingest the act, extract the forms. Idempotent.
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: runtime
    command: ["/bin/bash", "/app/scripts/bootstrap.sh"]
    env_file: [.env]
    environment:
      POSTGRES_HOST: postgres
      REDIS_HOST: redis
    volumes:
      - ./data:/data
      - ./scripts:/app/scripts:ro
      - modelcache:/home/nyaya/.cache
    depends_on:
      postgres: {condition: service_healthy}
      qdrant: {condition: service_healthy}
    profiles: [bootstrap]
    networks: [nyaya]

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://localhost:8000}
    ports:
      - "3000:3000"
    depends_on: [api]
    restart: unless-stopped
    networks: [nyaya]

  # ---- optional: `docker compose --profile monitoring up` ----
  prometheus:
    image: prom/prometheus:v3.0.1
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - promdata:/prometheus
    ports: ["9090:9090"]
    profiles: [monitoring]
    networks: [nyaya]

  grafana:
    image: grafana/grafana:11.4.0
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD:-admin}
      GF_AUTH_ANONYMOUS_ENABLED: "true"
    volumes:
      - ./monitoring/grafana:/etc/grafana/provisioning:ro
      - grafanadata:/var/lib/grafana
    ports: ["3001:3000"]
    depends_on: [prometheus]
    profiles: [monitoring]
    networks: [nyaya]

volumes:
  qdrantdata:
  pgdata:
  redisdata:
  modelcache:
  promdata:
  grafanadata:

networks:
  nyaya:
    driver: bridge
